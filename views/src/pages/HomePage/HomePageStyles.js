import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
   wrapper:{
    margin:"5rem auto",
   },
   button:{
      marginBottom:"1rem",
      float:"right"
   }
  }));